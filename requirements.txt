# Web framework
fastapi
uvicorn[standard]
python-multipart

# ML - no version pin, installs latest (matches local TF 2.21 used to save the model)
tensorflow-cpu
numpy
h5py

# Image processing
pillow
opencv-python-headless
