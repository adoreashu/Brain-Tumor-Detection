# Web framework — let pip choose compatible versions
fastapi
uvicorn[standard]
python-multipart

# ML — CPU-only TF 2.15 (Keras 2.x, no version conflicts)
tensorflow-cpu==2.15.0
numpy<1.25
h5py

# Image processing
pillow
opencv-python-headless
